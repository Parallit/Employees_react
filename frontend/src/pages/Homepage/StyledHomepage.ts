import { styled } from "styled-components"

export const Container = styled.div`
  padding: 45px 45px;
  color: #fff;
  background: rgba(0, 0, 0, 0.8);
  border-radius: 10px;
  box-shadow: 0 15px 25px rgba(0, 0, 0, 0.8);
`

export const TextProjectName = styled.h2`
  margin: 0 0 50px 0;
  padding: 0 20px;
  letter-spacing: 1px;
`

export const Text = styled.p`
  padding: 0 20px;
  letter-spacing: 2px;
`